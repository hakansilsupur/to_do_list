node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
npm-debug.log*
yarn-error.log*

# Android signing — a committed release keystore is an unrevocable key compromise
*.jks
*.keystore
keystore.properties
android/local.properties
# ...except the shared debug key, which is deliberately public so that every
# build shares one certificate and APKs can update over each other.
!android/app/debug.keystore
